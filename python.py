width = float(input("enter the width in Meters"))
    
length = float(input("enter length in Meters"))

depth = float(input("enter depth in Meters"))

result = depth * length * width * 2.3

result = round(result, 3)

def calculate():
  
   print(result)
   
   
calculate()
